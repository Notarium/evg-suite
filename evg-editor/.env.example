# Add local-only credentials here if a future integration needs them.
# Never commit real values; the project .gitignore excludes .env and credential files.
# OPENAI_API_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
